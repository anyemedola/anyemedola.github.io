import { styled } from '@mui/material/styles';

export const SectionInner = styled('div')({
  maxWidth: 1240,
  margin: '0 auto',
  padding: '104px 40px',
  '@media (max-width: 900px)': {
    padding: '64px 22px',
  },
});
